const KEY='fitguide-profile';
const saved=JSON.parse(localStorage.getItem(KEY)||'null');
const state={tab:'today',profile:saved||null,seconds:0,timer:null,step:0,form:{name:'',age:'',sex:'',height:'',weight:'',goal:'fat-loss',experience:'beginner',days:3,minutes:30,environment:'home',equipment:'none',diet:'balanced'}};
const main=document.getElementById('main'),sheet=document.getElementById('sheet'),sheetCard=document.getElementById('sheet-card');
const goals={"fat-loss":'Lose body fat',strength:'Build strength',muscle:'Build muscle',fitness:'Improve fitness',habit:'Build consistency'};
const workouts={beginner:[['Dead bug','3 × 8 / side','Core'],['Bodyweight squat','3 × 10','Lower body'],['Incline push-up','3 × 8','Upper body'],['Glute bridge','3 × 12','Posterior chain']],intermediate:[['Dead bug','3 × 10 / side','Core'],['Split squat','3 × 10 / side','Lower body'],['Push-up','3 × 10','Upper body'],['Hip hinge','3 × 12','Posterior chain']],advanced:[['Dead bug','3 × 12 / side','Core'],['Reverse lunge','4 × 10 / side','Lower body'],['Push-up','4 × 12','Upper body'],['Single-leg bridge','4 × 10 / side','Posterior chain']]};
function esc(v){return String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function plan(){const p=state.profile||{};const mins=Number(p.minutes)||30;const level=p.experience||'beginner';const list=workouts[level]||workouts.beginner;return {mins,level,list,title:p.goal==='fat-loss'?'Full body burn':p.goal==='strength'?'Strength foundation':p.goal==='muscle'?'Build & grow':p.goal==='fitness'?'Fitness foundation':'Build your base'}}
function today(){const p=state.profile;if(!p)return `<section class="welcome"><div class="eyebrow">FITNESS GUIDE</div><h1>Your plan.<br>Your pace.</h1><p class="sub">Tell us a little about you. We'll shape training and nutrition around your goal, time and setup.</p><button class="primary" data-action="onboard">Create my plan <span class="arrow">↗</span></button><div class="trust"><span>Personal</span><span>Adaptive</span><span>Simple</span></div></section>`;const q=plan();const pct=Math.min(100,Math.round((Number(p.completed)||0)/5*100));return `<section class="hero"><div class="eyebrow">TODAY · ${new Date().toLocaleDateString(undefined,{weekday:'long',month:'long',day:'numeric'})}</div><h1>Good to see<br>you, ${esc(p.name)}.</h1><p class="sub">${goals[p.goal]||'Your plan'} · ${q.mins} min · ${esc(p.environment)}</p><button class="primary" data-action="start-workout">Start today's session <span class="arrow">↗</span></button></section><article class="card focus"><div class="focus-copy"><div class="eyebrow">TODAY'S FOCUS</div><h2>${q.title}</h2><p class="sub">${q.list.length} movements · ${esc(p.experience)}</p></div><div class="focus-orbit"><span></span><i></i><b>${pct}%</b></div></article><div class="section-head"><div><div class="eyebrow">YOUR PLAN</div><h2>Built around you.</h2></div><button class="text-button" data-action="edit-profile">Edit</button></div><section class="grid two"><article class="card mini"><div class="eyebrow">TRAIN</div><strong>${p.days} days</strong><span>/ week</span></article><article class="card mini"><div class="eyebrow">TIME</div><strong>${q.mins}</strong><span>minutes</span></article></section><article class="card quote"><p>“The best plan is the one that fits your life well enough to repeat.”</p><span>FITNESS GUIDE · COACH NOTE</span></article>`}
function train(){const p=state.profile||{experience:'beginner',minutes:30,goal:'habit'};const q=plan();return `<section class="stat-hero"><div class="eyebrow">TRAIN</div><div class="big">Move<br>with intent.</div><p class="sub">${q.mins} minutes · ${esc(p.environment||'home')} · ${esc(p.experience||'beginner')}</p></section><article class="card focus"><div class="focus-copy"><div class="eyebrow">TODAY'S SESSION</div><h2>${q.title}</h2><p class="sub">Warm up, move well, then finish strong.</p></div><button class="primary" data-action="start-workout">Start <span class="arrow">↗</span></button></article><div class="section-head"><div><div class="eyebrow">SESSION</div><h2>${q.list.length} movements.</h2></div></div><div class="list">${q.list.map((x,i)=>`<button class="row" data-action="exercise" data-name="${esc(x[0])}" data-detail="${esc(x[1])} · ${esc(x[2])}"><span class="row-icon">${String(i+1).padStart(2,'0')}</span><span class="row-copy"><strong>${x[0]}</strong><small>${x[1]} · ${x[2]}</small></span><span class="chev">›</span></button>`).join('')}</div>`}
function nutrition(){const p=state.profile||{};const goal=goals[p.goal]||'Your goal';return `<section class="stat-hero"><div class="eyebrow">NUTRITION</div><div class="big">Eat for<br>your goal.</div><p class="sub">Simple meal guidance shaped by ${goal.toLowerCase()} and your preferences.</p></section><article class="card nutrition-hero"><div><div class="eyebrow">DAILY GUIDE</div><h2>Protein first.</h2><p class="sub">Build meals around a clear protein source, plants and a satisfying carb.</p></div><div class="nutri-circle"><strong>${p.diet==='vegetarian'?'VEG':'ALL'}</strong></div></article><div class="section-head"><div><div class="eyebrow">IDEAS</div><h2>Easy next meals.</h2></div></div><div class="list"><button class="meal" data-action="meal"><div><strong>${p.diet==='vegetarian'?'Paneer bhurji + roti':'Eggs + whole-grain toast'}</strong><small>Protein-led breakfast</small></div><b>01</b></button><button class="meal" data-action="meal"><div><strong>Dal + rice + vegetables</strong><small>Balanced lunch</small></div><b>02</b></button><button class="meal" data-action="meal"><div><strong>${p.diet==='vegetarian'?'Chana salad + curd':'Chicken + rice + salad'}</strong><small>Simple dinner</small></div><b>03</b></button></div>`}
function progress(){const p=state.profile||{};return `<section class="stat-hero"><div class="eyebrow">PROGRESS</div><div class="big">See the<br>change.</div><p class="sub">Track consistency, not perfection.</p></section><article class="card chart-card"><div class="card-top"><div><div class="eyebrow">TRAINING</div><h2>Week at a glance.</h2></div><span class="tiny">${p.days||3} target days</span></div><div class="chart">${[32,58,46,82,64,92,70].map((h,i)=>`<i style="--h:${h}%;--d:${i*70}ms"></i>`).join('')}</div><div class="days"><span>M</span><span>T</span><span>W</span><span>T</span><span>F</span><span>S</span><span>S</span></div></article><section class="grid two"><article class="card mini"><div class="eyebrow">GOAL</div><strong>${esc(goals[p.goal]||'Fitness')}</strong><span>${p.experience||'beginner'}</span></article><article class="card mini"><div class="eyebrow">CONSISTENCY</div><strong>${p.completed||0}</strong><span>sessions logged</span></article></section>`}
function you(){const p=state.profile;return `<section class="profile-head"><div class="profile-big">${esc((p?.name||'Y')[0]).toUpperCase()}</div><div><div class="eyebrow">YOUR PROFILE</div><h2>${esc(p?.name||'You')}</h2><p class="sub">${esc(goals[p?.goal]||'Set your goal')} · ${esc(p?.experience||'beginner')}</p></div></section><div class="list"><button class="row" data-action="edit-profile"><span class="row-icon">◎</span><span class="row-copy"><strong>Personal details</strong><small>${p?.age||'—'} years · ${p?.height||'—'} cm · ${p?.weight||'—'} kg</small></span><span class="chev">›</span></button><button class="row" data-action="edit-profile"><span class="row-icon">⌁</span><span class="row-copy"><strong>Training setup</strong><small>${p?.days||3} days · ${p?.minutes||30} min · ${esc(p?.environment||'home')}</small></span><span class="chev">›</span></button></div><div class="section-head"><div><div class="eyebrow">SETTINGS</div><h2>Preferences.</h2></div></div><article class="card"><div class="setting"><div><strong>Motion</strong><small>Subtle transitions and feedback</small></div><button class="switch on" data-action="toggle"><i></i></button></div><div class="setting"><div><strong>Daily reminders</strong><small>Keep your plan visible</small></div><button class="switch on" data-action="toggle"><i></i></button></div></article>`}
const views={today,train,nutrition,progress,you};
function render(){main.innerHTML=views[state.tab]();document.querySelectorAll('.tab').forEach(b=>b.classList.toggle('active',b.dataset.tab===state.tab));document.querySelector('.avatar').textContent=state.profile?(state.profile.name[0]||'Y').toUpperCase():'Y';main.classList.remove('page-in');requestAnimationFrame(()=>main.classList.add('page-in'));window.scrollTo({top:0,behavior:'smooth'})}
function openSheet(html){sheetCard.innerHTML=html;sheet.classList.add('open');sheet.setAttribute('aria-hidden','false')}
function closeSheet(){sheet.classList.remove('open');sheet.setAttribute('aria-hidden','true');if(timerId){clearInterval(timerId);timerId=null}}
function onboard(){state.step=0;state.form={name:'',age:'',sex:'',height:'',weight:'',goal:'fat-loss',experience:'beginner',days:3,minutes:30,environment:'home',equipment:'none',diet:'balanced'};renderOnboard()}
function renderOnboard(){const s=state.step;const f=state.form;const steps=[['What should we call you?','A plan feels better when it feels personal.'],['A little context.','Used only to shape your starting plan.'],['What are you working toward?','Choose the outcome that matters most right now.'],['How do you like to train?','We'll fit the session to your real week.'],['Where will you train?','Equipment changes the movements, not the quality of the plan.'],['Your starting plan.','Review the choices before we build your first week.']];let body='';if(s===0)body=`<label>Your name<input id="f-name" value="${esc(f.name)}" placeholder="Your name" autocomplete="name"></label>`;if(s===1)body=`<div class="field-grid"><label>Age<input id="f-age" type="number" min="13" max="100" value="${esc(f.age)}" placeholder="Years"></label><label>Height<input id="f-height" type="number" min="100" max="230" value="${esc(f.height)}" placeholder="cm"></label><label>Weight<input id="f-weight" type="number" min="35" max="300" value="${esc(f.weight)}" placeholder="kg"></label><label>Sex<select id="f-sex"><option value="">Prefer not to say</option><option value="female" ${f.sex==='female'?'selected':''}>Female</option><option value="male" ${f.sex==='male'?'selected':''}>Male</option></select></label></div>`;if(s===2)body=`<div class="choice-grid">${Object.entries(goals).map(([k,v])=>`<button class="choice ${f.goal===k?'selected':''}" data-pick="goal" data-value="${k}"><strong>${v}</strong><small>${k==='fat-loss'?'Reduce body fat':k==='strength'?'Get stronger':k==='muscle'?'Add muscle':k==='fitness'?'Build capacity':'Make training stick'}</small></button>`).join('')}</div>`;if(s===3)body=`<div class="choice-grid">${['beginner','intermediate','advanced'].map(v=>`<button class="choice ${f.experience===v?'selected':''}" data-pick="experience" data-value="${v}"><strong>${v[0].toUpperCase()+v.slice(1)}</strong><small>${v==='beginner'?'New to structured training':'Comfortable with structured training'}</small></button>`).join('')}</div><div class="field-grid compact"><label>Days / week<input id="f-days" type="number" min="1" max="7" value="${f.days}"></label><label>Minutes / session<input id="f-minutes" type="number" min="10" max="120" value="${f.minutes}"></label></div>`;if(s===4)body=`<div class="choice-grid">${[['home','Home'],['gym','Gym'],['outdoor','Outdoor']].map(([k,v])=>`<button class="choice ${f.environment===k?'selected':''}" data-pick="environment" data-value="${k}"><strong>${v}</strong><small>${k==='home'?'Simple, flexible setup':k==='gym'?'Machines and free weights':'Minimal equipment'}</small></button>`).join('')}</div><div class="choice-grid single">${[['none','No equipment'],['basic','Basic equipment'],['full','Full gym']].map(([k,v])=>`<button class="choice ${f.equipment===k?'selected':''}" data-pick="equipment" data-value="${k}"><strong>${v}</strong></button>`).join('')}</div><div class="choice-grid single">${[['balanced','Balanced'],['vegetarian','Vegetarian'],['high-protein','Higher protein']].map(([k,v])=>`<button class="choice ${f.diet===k?'selected':''}" data-pick="diet" data-value="${k}"><strong>${v}</strong></button>`).join('')}</div>`;if(s===5)body=`<div class="plan-preview"><div><span>GOAL</span><strong>${goals[f.goal]}</strong></div><div><span>TRAINING</span><strong>${f.days} days · ${f.minutes} min</strong></div><div><span>SETUP</span><strong>${f.environment} · ${f.equipment}</strong></div><div><span>NUTRITION</span><strong>${f.diet}</strong></div></div>`;openSheet(`<div class="onboard"><div class="sheet-handle"></div><div class="onboard-top"><span>${s+1} / 6</span><span>${Math.round((s+1)/6*100)}%</span></div><div class="progress-line"><i style="width:${(s+1)/6*100}%"></i></div><div class="eyebrow">PERSONALIZE</div><h2>${steps[s][0]}</h2><p class="sub">${steps[s][1]}</p>${body}<div class="actions"><button class="secondary" data-action="onboard-back" ${s===0?'disabled':''}>Back</button><button class="primary" data-action="onboard-next">${s===5?'Build my plan':'Continue'} <span class="arrow">↗</span></button></div></div>`)}
function saveForm(){const f=state.form;if(!f.name)f.name='You';state.profile={...f,completed:0};localStorage.setItem(KEY,JSON.stringify(state.profile));closeSheet();state.tab='today';render()}
function syncForm(){const f=state.form;['name','age','height','weight','sex','days','minutes'].forEach(k=>{const el=document.getElementById('f-'+k);if(el)f[k]=el.value});}
function startWorkout(){const q=plan();state.seconds=q.mins*60;openSheet(`<div class="sheet-handle"></div><div class="workout-mode"><div class="eyebrow">TODAY'S SESSION</div><h2>${q.title}</h2><div class="exercise-art"><i class="orb a"></i><i class="orb b"></i><i class="orb c"></i><span>MOVE</span></div><div class="timer" id="timer">${fmt(state.seconds)}</div><p class="sub center">Start when you're ready. Pause any time.</p><div class="actions"><button class="secondary" data-action="close-sheet">Exit</button><button class="primary" data-action="timer-toggle">Start</button></div></div>`)}
function fmt(s){return `${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`}
function timerToggle(btn){if(timerId){clearInterval(timerId);timerId=null;btn.textContent='Resume';return}btn.textContent='Pause';timerId=setInterval(()=>{state.seconds=Math.max(0,state.seconds-1);const e=document.getElementById('timer');if(e)e.textContent=fmt(state.seconds);if(state.seconds===0){clearInterval(timerId);timerId=null;btn.textContent='Complete';const p=state.profile;if(p){p.completed=(p.completed||0)+1;localStorage.setItem(KEY,JSON.stringify(p));}}},1000)}
document.addEventListener('click',e=>{const tab=e.target.closest('[data-tab]');if(tab){state.tab=tab.dataset.tab;render();return}const el=e.target.closest('[data-action],[data-pick]');if(!el)return;const a=el.dataset.action;if(el.dataset.pick){state.form[el.dataset.pick]=el.dataset.value;renderOnboard();return}if(a==='onboard')onboard();if(a==='onboard-next'){syncForm();if(state.step<5){state.step++;renderOnboard()}else saveForm()}if(a==='onboard-back'){syncForm();state.step=Math.max(0,state.step-1);renderOnboard()}if(a==='edit-profile')onboard();if(a==='profile'){state.tab='you';render()}if(a==='start-workout')startWorkout();if(a==='close-sheet')closeSheet();if(a==='timer-toggle')timerToggle(el);if(a==='toggle')el.classList.toggle('on');if(a==='exercise')openSheet(`<div class="sheet-handle"></div><div class="eyebrow">EXERCISE</div><h2>${esc(el.dataset.name)}</h2><p class="sub">${esc(el.dataset.detail)}</p><div class="exercise-art"><i class="orb a"></i><i class="orb b"></i></div><p class="sub">Move with control. Keep breathing steady. Stop if you feel pain.</p><button class="primary" data-action="close-sheet">Done</button>`);if(a==='meal')openSheet(`<div class="sheet-handle"></div><div class="eyebrow">MEAL IDEA</div><h2>${esc(el.querySelector('strong')?.textContent||'Meal')}</h2><p class="sub">Build the plate around protein, add vegetables or fruit, then choose a satisfying carbohydrate and a source of healthy fat.</p><button class="primary" data-action="close-sheet">Got it</button>')});
render();
